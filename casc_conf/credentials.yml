credentials:
    system:
      domainCredentials:
        - credentials:
            - usernamePassword:
                scope:    GLOBAL
                id:       github_deploy_token
                username: github_deploy_token
                password: ${github_deploy_token}