credentials:
    system:
      domainCredentials:
        - credentials:
            - usernamePassword:
                scope:    GLOBAL
                id:       github_personal_access_token
                username: JungStar
                password: ${github_personal_access_token}