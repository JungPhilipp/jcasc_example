credentials:
    system:
      domainCredentials:
        - credentials:
            - usernamePassword:
                scope:    GLOBAL
                id:       github_deploy_key
                username: github_deploy_key
                password: ${github_deploy_key}